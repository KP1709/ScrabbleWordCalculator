import "../styles/header.css"

export default function Header() {
    return (
        <header className="flex-centre" data-test="page-header">
            <h1>Scrabble Word Calculator</h1>
        </header>
    )
}